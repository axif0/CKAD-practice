An existing Deployment named `busybox` is running in namespace `rapid-goat`.

Do NOT delete the existing Deployment. Failure to do so will result in a zero score.

Tasks:

1. Change the container name to `musl`.
2. Change the container image to `busybox:musl`.
3. Ensure the changes are rolled out successfully.

Verify the rollout status and confirm the updated container name and image.
