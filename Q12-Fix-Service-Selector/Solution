# Check current state
kubectl get pods --show-labels
kubectl get svc web-svc -o yaml
kubectl get endpoints web-svc

# Fix selector to match pod labels (app=web-app)
kubectl patch svc web-svc -p '{"spec":{"selector":{"app":"web-app"}}}'

# Verify
kubectl get endpoints web-svc
