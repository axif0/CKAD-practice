In namespace `default`, a Deployment named `nginx` exists.

Configure the Deployment so that the `nginx` container uses a readinessProbe with the following requirements:

- Probe type: `httpGet`
- Path: `/ready`
- Port: `80`
- `initialDelaySeconds`: 5

Do not modify any other settings.

Verify the readiness probe configuration by describing the Pod.
