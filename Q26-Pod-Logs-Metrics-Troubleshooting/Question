Task 1:

Deploy the Pod `winter` to the cluster using the manifest file at:
`/opt/ckadnov2025/winter.yaml`

Retrieve all currently available application logs from the running Pod and store them in the file:
`/opt/ckadnov2025/log_Output.txt` (the file has already been created)

---

Task 2:

From the Pods running in namespace `cpu-stress`, identify the Pod consuming the most CPU.

Write the name of that Pod (name only, no headers) to the file:
`/opt/ckadnov2025/pod.txt` (the file has already been created)
