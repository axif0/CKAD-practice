A Pod within the Deployment named `honeybee-deployment` in namespace `gorilla` is logging errors.

Look at the logs to identify error messages.

Find errors including:
User "system:serviceaccount:gorilla:default" cannot list resource "pods" [...] in the namespace "gorilla"

Update the Deployment `honeybee-deployment` to resolve the errors in the logs of the Pod.

Note:
- The honeybee-deployment's manifest file can be found at /home/candidate/prompt-escargot/honeybee-deployment.yaml
