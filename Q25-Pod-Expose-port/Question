A web application requires a specific version of Redis to be used as a cache.

Task:

Create a Pod with the following specifications and leave it running when complete:

- Name: `cache`
- Namespace: `web` (the namespace has already been created)
- Image: `redis:3.2`
- Expose container port `6379`

Verify that the Pod is in `Running` state.
